import { css } from "styled-components";

const svgStyles = ({ height, width }) => {
  return css`
    height: ${height};
    width: ${width};
    // margin-left: 10px;
  `;
};

export default svgStyles;
