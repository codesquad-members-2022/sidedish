spring:
  datasource:
    url: jdbc:mysql://localhost:13306/sidedishDB
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql