const VISIBLE_CARD_COUNT = {
  default: 4,
  small: 5,
};

const MEDIUM_CARD_WIDTH = {
  default: 302,
  small: 160,
};

const MEDIUM_CARD_MARGIN = {
  default: 24,
  small: 16,
};

const CARD_SIZE = {
  default: 'medium',
  small: 'small',
};

const Slider_WIDTH = {
  default: 1300,
  small: 864,
};

export { VISIBLE_CARD_COUNT, MEDIUM_CARD_WIDTH, MEDIUM_CARD_MARGIN, CARD_SIZE, Slider_WIDTH };
